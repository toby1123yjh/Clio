# Screenshots

Drop the README's screenshots here, then replace the `placehold.co` placeholder
URLs in `../../README.md` (and `README.zh-CN.md`) with the local paths.

Suggested shots (PNG, ~1600px wide, light theme):

| File | What to capture |
|---|---|
| `hero.png` | The in-page Rail answering a question, with a citation visible |
| `capture.png` | Saving the current page or a selection to memory |
| `memory.png` | The Memory Library — search results over captured pages |
| `cite.png` | Clicking a citation jumping back to the source highlight |

Tip: use a clean, recognizable article page as the backdrop so the captured
content reads well in the shot.
